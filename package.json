{
  "name": "mamutuelle-frontend",
  "version": "1.0.0",
  "description": "Frontend pour le système de gestion MaMutuelle",
  "main": "script.js",
  "scripts": {
    "dev": "live-server --port=3000",
    "build": "mkdir -p dist && cp *.html dist/ && cp -r css dist/ && cp -r js dist/",
    "minify": "npm run build && find dist -name '*.js' -exec uglifyjs {} -o {} \\;",
    "test": "echo \"Aucun test frontend configuré\" && exit 0"
  },
  "keywords": ["mutuelle", "frontend", "dashboard"],
  "author": "Équipe MaMutuelle",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.2",
    "uglify-js": "^3.17.4"
  },
  "dependencies": {
    "bootstrap": "^5.3.0",
    "chart.js": "^4.3.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}